<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/main"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".rfi.activity.RFIHistoryActivity">

    <include layout="@layout/layout_toolbar"
        android:id="@+id/rfi_history_toolbar"/>



    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/rfi_history_recycler_view"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@id/rfi_history_toolbar"
        tools:listitem="@layout/layout_monitoring_inspection_history_list" />


    <TextView
        android:id="@+id/rfi_history_no_data_text_view"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerInParent="true"
        android:visibility="gone"
        android:textColor="@color/redColor"
        android:textStyle="bold"
        android:text="No history found"/>

    <ProgressBar
        android:id="@+id/rfi_progress_bar"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:visibility="gone"
        android:layout_centerInParent="true"/>

</RelativeLayout>